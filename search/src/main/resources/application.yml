eureka:
  server:
    account: admin
    password: root
  client:
    service-url:
      defaultZone: http://${eureka.server.account}:${eureka.server.password}@localhost:8021/eureka

version: v1

spring:
  application:
    name: SEARCH-${version}

server:
  port: 8032
